import React from 'react';

const Header = () => (
    <header className="header" data-testid="header">
        <div className="container">
            <h1 className="logo"><span className="sr-only">Blue Prism Schedules</span></h1>
        </div>
    </header>
);

export default Header;
