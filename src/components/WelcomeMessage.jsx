import React from 'react';

const WelcomeMessage = () => (
    <p data-testid="welcome-message">Please click &apos;View Entries&apos; on a schedule to view the log entries.</p>
);

export default WelcomeMessage;
